dataset:
  path: src/data/stock_data/ABBV.csv
model:
  name: LSTM
  hidden_dim: 50
  dropout: 0.2
  parameters:
    learning_rate: 0.0001
    epochs: 100
    batch_size: 10
training:
  look_back: 60
  validation_split: 0.2
  early_stopping_patience: 10
